apiVersion: v1
data:
  config.json: |
    {
      "app": {
        "version": "2.0.2",
        "environment": "production"
      },
      "logging": {
        "level": "info",
        "format": "json"
      },
      "message": {
        "hello": "Hello.!! from ConfigMap."
      }
    }
kind: ConfigMap
metadata:
  name: config
